#include <stdio.h>

int main(void) {
    int n;
    scanf("%d", &n);

    int result = 0, num;
    for (int i = 0; i < n; i++) {
        scanf("%d", &num);
        result ^= num; // XOR with current number
    }

    printf("%d\n", result);
    return 0;
}
